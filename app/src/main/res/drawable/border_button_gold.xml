<!-- res/drawable/border_button_gold.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Fundo transparente -->
    <corners android:radius="8dp" /> <!-- Bordas arredondadas -->
    <stroke
        android:width="2dp"
        android:color="@color/goldww" /> <!-- Cor do contorno -->
</shape>
