<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WalletWiz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColor">@color/goldww</item>
        <item name="colorPrimary">@color/purpleww</item>
        <item name="android:textColorPrimary">@color/purpleww</item>
        <item name="android:fontFamily">@font/bree_serif</item>

    </style>

    <style name="Theme.WalletWiz" parent="Base.Theme.WalletWiz" />
</resources>